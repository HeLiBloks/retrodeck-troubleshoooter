#!/usr/bin/env python3
"""Entrypoint that needs no install: it puts src/ on the path itself."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent / "src"))

from rdtroubleshoot.cli import entrypoint

entrypoint()
